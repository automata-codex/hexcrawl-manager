---
import { canAccess, getCurrentUserRole } from '../../utils/auth';
import { SCOPES } from '../../utils/constants';

import type { Scope } from '@achm/schemas';

// Restricts content to users with the "gm" role.

const role = getCurrentUserRole(Astro.locals);
---

{canAccess(role, [SCOPES.GM]) && <slot />}
