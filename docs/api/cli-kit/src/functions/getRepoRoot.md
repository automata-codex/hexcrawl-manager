# Function: getRepoRoot()

> **getRepoRoot**(): `string`

Defined in: [packages/cli-kit/src/repo/get-repo-root.ts:7](https://github.com/alexgs/skyreach/blob/develop/packages/cli-kit/src/repo/get-repo-root.ts#L7)

Returns the repository root directory, respecting the REPO_ROOT env override.
Falls back to config if not set.

## Returns

`string`
