# Function: hexSort()

> **hexSort**(`hexIdA`, `hexIdB`): `number`

Defined in: [packages/core/src/hexes/hex-sort.ts:14](https://github.com/alexgs/skyreach/blob/develop/packages/core/src/hexes/hex-sort.ts#L14)

Sorts two hex IDs first by column (alphabetically) and then by row (numerically).

## Parameters

### hexIdA

`string`

### hexIdB

`string`

## Returns

`number`
