# Function: resolveDataPath()

> **resolveDataPath**(`rel`): `string`

Defined in: [packages/data/src/paths.ts:7](https://github.com/alexgs/skyreach/blob/develop/packages/data/src/paths.ts#L7)

Resolve a path relative to the repo's `data/` directory.

## Parameters

### rel

`string`

## Returns

`string`
